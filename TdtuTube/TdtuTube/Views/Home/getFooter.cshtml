@model IEnumerable<TdtuTube.Models.HomeMenu>

<div class="flex flex-col px-6 py-2">
    <div class="flex flex-wrap">
        @foreach (var i in Model)
        {
            <a class="text-xs text-gray-400" style="margin-right: 6px;" href="/@i.meta">@i.name</a>
        }
    </div>
</div>